{
  "name": "erp_ani_enterprise",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker-compose up -d mongo && concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "run:full": "docker-compose up --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}